"""Velocity command source for stair-climbing.

Matches the paper's curriculum: forward-only commands in stage 1 (0.0 - 0.7 m/s),
broader heading sampling once policy + perception are joint-trained.
"""

from __future__ import annotations

# Placeholder — subclass `isaaclab.envs.mdp.UniformVelocityCommandCfg` once
# the per-stage ranges from §III-D Table II are finalized.
